FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm install

COPY . .

EXPOSE 5173

# --host per accettare connessioni dall'esterno del container
CMD ["npx", "vite", "--host", "0.0.0.0"]
